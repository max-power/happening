<%= semantic_form_for [parent, resource] do |f| %>
  <%= f.semantic_errors  %>
  
  <%= f.inputs do %>
    <%= f.input :name %>
    <%= f.input :info, as: :text %>
  <% end %>

  <%= f.actions %>
<% end %>
